


<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
  <div class="container-fluid">
    <a
      class="navbar-brand"
      href="/listings"
      style="
        font-weight: 900;
        color: #ff3442;
        font-size: x-large;
        user-select: none;
      "
      >AirBnb</a
    >
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse  z-1" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link" href="/listings">Explore</a>
      </div>

      <div class="navbar-nav ms-auto">

        <form class="d-flex" role="search" method="get" action="/listings/search">
           <input
            class="form-control me-2 search-inp"
            type="text"
            placeholder="Search By Country"
            aria-label="Search"
            name="country"
          />
          <button class="btn search-inp">Search</button>
           
        </form>
      </div>
      <div class="navbar-nav ms-auto flex justify-center items-center">
        <a class="nav-link" href="/listings/new">AirBnb Your Home</a>
        <!-- if User exist then show only LOgOut button -->
        <% if (currUser) { %>
        <a class="nav-link" href="/users/logout"
          ><button class="btn">LogOut</button></a
        >
        <% } %>

        <!-- If user not exist or login then show Login and SignUp -->
        <% if (!currUser) { %>
        <a class="nav-link" href="/users/signup"><b>Sign Up</b></a>
        <a class="nav-link" href="/users/login"><b>Login</b></a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
